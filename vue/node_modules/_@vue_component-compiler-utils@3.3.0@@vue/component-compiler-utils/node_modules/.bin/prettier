#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../_prettier@2.5.1@prettier/bin-prettier.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../../_prettier@2.5.1@prettier/bin-prettier.js" "$@"
  ret=$?
fi
exit $ret
